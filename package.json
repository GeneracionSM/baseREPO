{
    "scripts": {
        "install-all": "cd web_gen_sm && npm install",

        "front:dev": "cd web_gen_sm && next dev",
        "front:build": "next build",
        "front:start": "next start",
        "front:lint": "next lint"
    }
}
